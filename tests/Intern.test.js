const Intern = require('../lib/Intern');

describe("Intern", () => {
    describe("test1", () => {
        it("should do something, but I'm not sure what yet.",
        () => {
            const obj = new Intern();

            expect().toEqual();
        });
    });
});